diff -rupN original/usr/share/jolla-calendar/cover/CalendarCover.qml patch/usr/share/jolla-calendar/cover/CalendarCover.qml
--- original/usr/share/jolla-calendar/cover/CalendarCover.qml	2015-06-08 07:29:53.000000000 +0100
+++ patch/usr/share/jolla-calendar/cover/CalendarCover.qml	2015-06-08 07:46:53.000000000 +0100
@@ -4,6 +4,7 @@ import org.nemomobile.time 1.0
 import org.nemomobile.calendar 1.0
 
 CoverBackground {
+property int indexView: 0
     Label {
         //% "New event"
         text: qsTrId("calendar-la-new_event")
@@ -20,6 +21,7 @@ CoverBackground {
     }
 
     CoverActionList {
+enabled: eventList.count <= 2 
         CoverAction {
             iconSource: "image://theme/icon-cover-new"
             onTriggered: {
@@ -29,9 +31,31 @@ CoverBackground {
             }
         }
     }
+CoverActionList {
+enabled: eventList.count > 2 
+        CoverAction {
+            iconSource: "image://theme/icon-cover-new"
+            onTriggered: {
+                app.activate()
+                app.showMainPage(PageStackAction.Immediate)
+                pageStack.push("../pages/EventEditPage.qml", {}, PageStackAction.Immediate)
+            }
+        }
+CoverAction {
+            iconSource: 'image://theme/icon-cover-next'
+            onTriggered: {
+                indexView = (indexView+2)
+if(indexView >= eventList.count) {
+indexView = 0
+}
+                eventList.positionViewAtIndex(indexView, ListView.Beginning)
+            }
+        }
+    }
+
     Column {
         x: Theme.paddingLarge
-        spacing: Theme.paddingSmall
+        spacing: Theme.paddingExtraSmall
         width: parent.width - 2*Theme.paddingLarge
         DateLabel {
             id: dateLabel
@@ -82,6 +106,7 @@ CoverBackground {
                         if (event.allDay || now < occurrence.endTime) {
                             activeAndComing.append({ displayLabel: event.displayLabel, allDay: event.allDay,
                                                        startTime: occurrence.startTime, endTime: occurrence.endTime,
+evDay: occurrence.time,
                                                        color: event.color })
 
                             if (!event.allDay && (nextEnding == undefined || occurrence.endTime < nextEnding)) {
@@ -108,6 +133,7 @@ CoverBackground {
                 id: allEvents
                 startDate: wallClock.time
                 onUpdated: eventUpdater.update()
+                endDate: QtDate.addDays(wallClock.time, 7)  
             }
 
             ListView {
@@ -120,7 +146,7 @@ CoverBackground {
                 interactive: false
                 width: parent.width
                 height: 2*itemHeight + 2*spacing
-                spacing: Theme.paddingLarge
+                spacing: Theme.paddingExtraSmall
                 visible: count > 0
 
                 delegate: CoverEventItem {
@@ -128,7 +154,7 @@ CoverBackground {
                     allDay: model.allDay
                     startTime: model.startTime
                     endtime: model.endTime
-                    activeDay: wallClock.time
+                    activeDay: model.evDay
                     color: model.color
                     Component.onCompleted: eventList.itemHeight = height
                 }
diff -rupN original/usr/share/jolla-calendar/cover/CoverEventItem.qml patch/usr/share/jolla-calendar/cover/CoverEventItem.qml
--- original/usr/share/jolla-calendar/cover/CoverEventItem.qml	2015-06-08 07:29:53.000000000 +0100
+++ patch/usr/share/jolla-calendar/cover/CoverEventItem.qml	2015-06-08 07:46:53.000000000 +0100
@@ -1,7 +1,6 @@
 import QtQuick 2.0
 import Sailfish.Silica 1.0
 import "../pages"
-
 Row {
     property alias eventName: nameLabel.text
     property alias allDay: timeLabel.allDay
@@ -9,7 +8,6 @@ Row {
     property alias endtime: timeLabel.endTime
     property alias activeDay: timeLabel.activeDay
     property alias color: rectangle.color
-
     spacing: Theme.paddingSmall
 
     Rectangle {
@@ -23,6 +21,39 @@ Row {
     Column {
         id: labelColumn
         spacing: -Theme.paddingSmall
+
+        Text {
+           id: label
+
+           function fdw(d) {
+              var t = new Date
+              var t2 = new Date(d)
+              var today = new Date(t.getFullYear(), t.getMonth(), t.getDate())
+              var day = new Date(t2.getFullYear(), t2.getMonth(), t2.getDate())
+              var delta = (day - today) / 86400000
+              if (delta == 0) {
+                  //% "Today"
+                  return qsTrId("calendar-today")
+              }else if (delta == 1) {
+                  //% "Tomorrow"
+                  return qsTrId("calendar-tomorrow")
+              } else {
+                  //: Long date pattern without year. Used e.g. in month view.
+                  //% "d MMMM"
+                  return capitalize(Qt.formatDate(d, qsTrId("calendar-date_pattern_date_month")))
+              }
+          }
+
+          function capitalize(string) {
+              return string.charAt(0).toUpperCase() + string.substr(1)
+          }
+
+
+          text: fdw(startTime)
+          opacity: 0.6
+          font.pixelSize: Theme.fontSizeSmall
+          color: Theme.primaryColor
+        }
         EventTimeLabel {
             id: timeLabel
             opacity: 0.6
